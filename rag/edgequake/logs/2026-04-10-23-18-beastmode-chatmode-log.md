Task logs

Actions: Revalidated the Mission 03 implementation, fixed the remaining `docs/features.md` merge state, hardened `Makefile` background startup and frontend PID handling, updated README/tutorial/troubleshooting docs for the `vision`/`edgeparse` backend model, and ran the CI-scope gates: `cargo fmt --all -- --check`, `cargo clippy --workspace --lib -- -D warnings`, `cargo test --workspace --lib --no-fail-fast`, `CARGO_BUILD_JOBS=1 cargo build --workspace --release --lib`, `RUSTDOCFLAGS='-A rustdoc::broken_intra_doc_links -D warnings' cargo doc --workspace --no-deps`, and `bun run build`.
Decisions: Kept the backend precedence as `per-upload override -> workspace default -> EDGEQUAKE_PDF_PARSER_BACKEND -> vision`; treated low-content EdgeParse results as warnings rather than silent fallback; used `/tmp` PID files instead of the tracked `edgequake_webui/build_pid.txt`; accepted the local release-build `SIGKILL` as a machine resource issue and verified the same CI build command succeeds with `CARGO_BUILD_JOBS=1`.
Next steps: Stage the full mission change set, commit it, and verify `git status` is clean after the commit.
Lessons/insights: Mission 03 is functionally integrated and CI-scope clean; the only blunt caveat is that background process persistence cannot be truthfully revalidated across separate tool invocations in this harness because the tool reaps child processes after the session exits.
